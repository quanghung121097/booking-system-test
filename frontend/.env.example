# ── Frontend — file mẫu ─────────────────────────────────────────────────────
#
#   cp .env.example .env
#
# Chọn URL API phù hợp cách bạn chạy project:

# Docker (Nginx reverse proxy):
VITE_API_URL=http://localhost:8080/api

# Local không Docker (php artisan serve):
# VITE_API_URL=http://localhost:8000/api
